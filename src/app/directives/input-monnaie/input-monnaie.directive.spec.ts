import { InputMonnaieDirective } from './input-monnaie.directive';

describe('InputMonnaieDirective', () => {
  it('should create an instance', () => {
    const directive = new InputMonnaieDirective();
    expect(directive).toBeTruthy();
  });
});
